---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: instrumentationoptions.odigos.io
spec:
  group: odigos.io
  names:
    kind: InstrumentationOption
    listKind: InstrumentationOptionList
    plural: instrumentationoptions
    singular: instrumentationoption
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InstrumentationOption is the Schema for the instrumentationoptions
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InstrumentationOptionSpec defines the desired state of InstrumentationOption
              Each field in the struct will be converted to an OpenAPI v3 schema with
              the comments used as the description.
            properties:
              filters:
                description: Filters define how to apply the instrumentation options
                items:
                  description: InstrumentationOptionFilter defines a filter for applying
                    instrumentation options
                  properties:
                    key:
                      description: Key is the attribute key to filter (e.g., 'http.route',
                        'url.path')
                      type: string
                    matchType:
                      description: MatchType is the type of match (e.g., 'equals',
                        'startsWith')
                      type: string
                    matchValue:
                      description: MatchValue is the value to match against
                      type: string
                  required:
                  - key
                  - matchType
                  - matchValue
                  type: object
                type: array
              instrumentationLibraries:
                description: InstrumentationLibraries is a list of instrumentation
                  libraries to which this option applies.
                items:
                  description: InstrumentationLibrary represents a library for instrumentation
                  properties:
                    instrumentationLibraryName:
                      description: InstrumentationLibraryName is the name of the instrumentation
                        library
                      type: string
                    language:
                      description: Language is the programming language of the library
                      type: string
                  required:
                  - instrumentationLibraryName
                  - language
                  type: object
                type: array
              optionName:
                description: OptionName is the name of the option
                type: string
              optionValueBoolean:
                description: OptionValueBoolean is the boolean value of the option
                type: boolean
              workloads:
                description: Workloads is an optional list of k8s ns+kind+name to
                  which this option applies. If not specified, the option applies
                  to all workloads.
                items:
                  properties:
                    kind:
                      description: Kind is the k8s kind of the workload, e.g., 'Deployment'
                      enum:
                      - Deployment
                      - DaemonSet
                      - StatefulSet
                      type: string
                    name:
                      description: Name is the name of the k8s object of the workload
                      type: string
                    namespace:
                      description: Namespace is the k8s namespace of the workload
                      type: string
                  required:
                  - kind
                  - name
                  - namespace
                  type: object
                type: array
            required:
            - instrumentationLibraries
            - optionName
            type: object
          status:
            description: InstrumentationOptionStatus defines the observed state of
              InstrumentationOption
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
