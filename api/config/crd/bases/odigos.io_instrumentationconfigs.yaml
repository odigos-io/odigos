---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: instrumentationconfigs.odigos.io
spec:
  group: odigos.io
  names:
    kind: InstrumentationConfig
    listKind: InstrumentationConfigList
    plural: instrumentationconfigs
    singular: instrumentationconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InstrumentationConfig is the Schema for the instrumentationconfig
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InstrumentationConfigSpec defines the desired state of InstrumentationConfig
              Each field in the struct will be converted to an OpenAPI v3 schema with
              the comments used as the description.
            properties:
              config:
                description: config for this workload. the config is a list to allow
                  for multiple config options and values to be applied. the list is
                  processed in order, and the first matching config is applied.
                items:
                  description: WorkloadInstrumentationConfig defined a single config
                    option to apply on a workload, along with it's value, filters
                    and instrumentation libraries
                  properties:
                    instrumentationLibraries:
                      description: a list of instrumentation libraries to apply this
                        setting to if a library is not in this list, the setting should
                        not apply to it and should be cleared.
                      items:
                        description: InstrumentationLibrary represents a library for
                          instrumentation
                        properties:
                          instrumentationLibraryName:
                            description: InstrumentationLibraryName is the name of
                              the instrumentation library
                            type: string
                          language:
                            description: Language is the programming language of the
                              library
                            type: string
                        required:
                        - instrumentationLibraryName
                        - language
                        type: object
                      type: array
                    optionKey:
                      description: OptionKey is the name of the option This value
                        is transparent to the CRD and is passed as-is to the SDK.
                      type: string
                    optionValueBoolean:
                      description: OptionValueBoolean is the boolean value of the
                        option if it is a boolean
                      type: boolean
                  required:
                  - instrumentationLibraries
                  - optionKey
                  type: object
                type: array
              workload:
                description: Each object describes a single workload configuration
                properties:
                  kind:
                    description: Kind is the k8s kind of the workload, e.g., 'Deployment'
                    enum:
                    - Deployment
                    - DaemonSet
                    - StatefulSet
                    type: string
                  name:
                    description: Name is the name of the k8s object of the workload
                    type: string
                  namespace:
                    description: Namespace is the k8s namespace of the workload
                    type: string
                required:
                - kind
                - name
                - namespace
                type: object
            required:
            - config
            - workload
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
