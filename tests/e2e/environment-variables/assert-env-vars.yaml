apiVersion: odigos.io/v1alpha1
kind: InstrumentationConfig
metadata:
  name: deployment-nodejs-dockerfile-env
  namespace: default
  (ownerReferences[?kind == 'Deployment' && name == 'nodejs-dockerfile-env']):
    - apiVersion: apps/v1
      kind: Deployment
      name: nodejs-dockerfile-env
  (ownerReferences[?kind == 'Source' && name == 'default']):
    - apiVersion: odigos.io/v1alpha1
      kind: Source
      name: default
status:
  runtimeDetailsByContainer:
    - containerName: nodejs-dockerfile-env
      envFromContainerRuntime:
        - name: NODE_OPTIONS
          value: "--require /app/execute_before.js --max-old-space-size=256"
      language: javascript
---
apiVersion: odigos.io/v1alpha1
kind: InstrumentationConfig
metadata:
  name: deployment-nodejs-manifest-env
  namespace: default
  (ownerReferences[?kind == 'Deployment' && name == 'nodejs-manifest-env']):
    - apiVersion: apps/v1
      kind: Deployment
      name: nodejs-manifest-env
  (ownerReferences[?kind == 'Source' && name == 'default']):
    - apiVersion: odigos.io/v1alpha1
      kind: Source
      name: default
status:
  runtimeDetailsByContainer:
    - containerName: nodejs-manifest-env
# the env var is present in the pod manifest, hence the container runtime value
# is not relevant and not populated
      (envFromContainerRuntime == null): true
      language: javascript
---
apiVersion: odigos.io/v1alpha1
kind: InstrumentationConfig
metadata:
  name: deployment-java-supported-docker-env
  namespace: default
  (ownerReferences[?kind == 'Deployment' && name == 'java-supported-docker-env']):
    - apiVersion: apps/v1
      kind: Deployment
      name: java-supported-docker-env
  (ownerReferences[?kind == 'Source' && name == 'default']):
    - apiVersion: odigos.io/v1alpha1
      kind: Source
      name: default
status:
  runtimeDetailsByContainer:
    - containerName: java-supported-docker-env
      language: java
      envFromContainerRuntime:
        - name: LD_PRELOAD
          value: ""
---
apiVersion: odigos.io/v1alpha1
kind: InstrumentationConfig
metadata:
  name: deployment-java-supported-manifest-env
  namespace: default
  (ownerReferences[?kind == 'Deployment' && name == 'java-supported-manifest-env']):
    - apiVersion: apps/v1
      kind: Deployment
      name: java-supported-manifest-env
  (ownerReferences[?kind == 'Source' && name == 'default']):
    - apiVersion: odigos.io/v1alpha1
      kind: Source
      name: default
status:
  runtimeDetailsByContainer:
    - containerName: java-supported-manifest-env
      language: java
# the env var is present in the pod manifest, hence the container runtime value
# is not relevant and not populated
      (envFromContainerRuntime == null): true
---
apiVersion: odigos.io/v1alpha1
kind: InstrumentationConfig
metadata:
  name: deployment-python-alpine
  namespace: default
  (ownerReferences[?kind == 'Deployment' && name == 'python-alpine']):
    - apiVersion: apps/v1
      kind: Deployment
      name: python-alpine
  (ownerReferences[?kind == 'Source' && name == 'default']):
    - apiVersion: odigos.io/v1alpha1
      kind: Source
      name: default
status:
  runtimeDetailsByContainer:
    - containerName: python-alpine
# the env var is present in the pod manifest, hence the container runtime value
# is not relevant and not populated
      (envFromContainerRuntime == null): true
      language: python
---
