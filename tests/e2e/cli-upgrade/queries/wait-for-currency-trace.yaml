apiVersion: e2e.tests.odigos.io/v1
kind: TraceTest
description: This test waits for a trace that goes from frontend to currency services
query: |
  length([?span.serviceName == 'frontend']) > `0` &&
  length([?span.serviceName == 'currency']) > `0`
expected:
  count: 1
