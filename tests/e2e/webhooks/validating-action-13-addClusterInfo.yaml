apiVersion: odigos.io/v1alpha1
kind: Action
metadata:
  name: 03-action-addclusterinfo
  namespace: odigos-test
spec:
  signals:
  - TRACES
  addClusterInfo:
    clusterAttributes:
    - attributeName: foo
      attributeStringValue: bar
