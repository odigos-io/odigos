apiVersion: v1
kind: Namespace
metadata:
  name: browser-demo
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-html
  namespace: browser-demo
data:
  index.html: |
    <!doctype html>
    <html>
      <head>
        <title>Odigos Browser Demo</title>
      </head>
      <body>
        <h1>Odigos Browser Instrumentation Demo</h1>
        <button id="ping" onclick="fetch('/').then(() => console.log('pinged'))">Ping</button>
      </body>
    </html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: browser-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: frontend-html
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: browser-demo
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
