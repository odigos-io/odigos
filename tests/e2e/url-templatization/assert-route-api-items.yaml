apiVersion: e2e.tests.odigos.io/v1
kind: TraceTest
description: url-demo-app span with http.route /api/items/{itemId}
query: |
  length([?span.serviceName == 'url-demo-app' && span.spanAttributes."http.route" == '/api/items/{itemId}']) > `0`
expected:
  minimum: 1
