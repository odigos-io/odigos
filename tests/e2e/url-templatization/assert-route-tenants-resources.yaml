apiVersion: e2e.tests.odigos.io/v1
kind: TraceTest
description: url-demo-app span with http.route /v1/tenants/{tenantId}/resources/{resourceId}
query: |
  length([?span.serviceName == 'url-demo-app' && span.spanAttributes."http.route" == '/v1/tenants/{tenantId}/resources/{resourceId}']) > `0`
expected:
  minimum: 1
