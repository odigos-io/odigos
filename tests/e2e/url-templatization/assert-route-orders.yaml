apiVersion: e2e.tests.odigos.io/v1
kind: TraceTest
description: url-demo-app span with http.route /api/users/{userId}/orders/{orderId}
query: |
  length([?span.serviceName == 'url-demo-app' && span.spanAttributes."http.route" == '/api/users/{userId}/orders/{orderId}']) > `0`
expected:
  minimum: 1
