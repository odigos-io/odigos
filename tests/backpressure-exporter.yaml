apiVersion: odigos.io/v1alpha1
kind: Destination
metadata:
  name: mockdestination
  namespace: odigos-system
spec:
  data:
    MOCK_RESPONSE_DURATION_MS: "100"
    MOCK_REJECT_FRACTION: "0.25"
  destinationName: mock-back-pressure
  signals:
  # - TRACES
  # - METRICS
  - LOGS
  type: mock
