apiVersion: batch/v1
kind: Job
metadata:
  name: currencybot
  namespace: default
spec:
  template:
    metadata:
      annotations:
        workload: job
      labels:
        app: currencybot
    spec:
      restartPolicy: Never
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      containers:
        - name: curl
          image: curlimages/curl:8.4.0
          imagePullPolicy: IfNotPresent
          command: ['curl']
          args: ['-s', '-X', 'GET', 'http://frontend:8080/currency']
