[package]
name = "rust-detection-test"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
axum = "0.7"

[profile.release]
strip = false

[profile.release-stripped]
inherits = "release"
strip = "symbols"

