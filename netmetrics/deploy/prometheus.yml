global: { scrape_interval: 5s }
scrape_configs:
  - job_name: netmetrics-enriched
    static_configs: [{ targets: ["localhost:9100"] }]
  - job_name: obi-raw
    static_configs: [{ targets: ["localhost:8999"] }]
