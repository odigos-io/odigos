import React from 'react';
import {
  MultiInputTable,
  MultiInputTableProps,
} from '@keyval-dev/design-system';

export function KeyvalMultiInputTable(
  props: MultiInputTableProps
): JSX.Element {
  return <MultiInputTable {...props} />;
}
