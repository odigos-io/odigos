package graph

// This file will be automatically regenerated based on the schema, any resolver implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.70

import (
	"context"
	"encoding/json"
	"fmt"

	"github.com/odigos-io/odigos/frontend/graph/model"
	"github.com/odigos-io/odigos/frontend/services/profiles"
)

// EnableSourceProfiling is the resolver for the enableSourceProfiling field.
func (r *mutationResolver) EnableSourceProfiling(ctx context.Context, namespace string, kind string, name string) (*model.EnableProfilingResult, error) {
	if r.ProfileStore == nil {
		return nil, fmt.Errorf("profiling is not available")
	}
	result, err := profiles.EnableProfilingForSource(r.ProfileStore, namespace, kind, name)
	if err != nil {
		return nil, err
	}
	return &model.EnableProfilingResult{
		Status:      result.Status,
		SourceKey:   result.SourceKey,
		MaxSlots:    result.MaxSlots,
		ActiveSlots: result.ActiveSlots,
	}, nil
}

// ReleaseSourceProfiling is the resolver for the releaseSourceProfiling field.
func (r *mutationResolver) ReleaseSourceProfiling(ctx context.Context, namespace string, kind string, name string) (*model.ReleaseProfilingResult, error) {
	if r.ProfileStore == nil {
		return nil, fmt.Errorf("profiling is not available")
	}
	result, err := profiles.ReleaseProfilingForSource(r.ProfileStore, namespace, kind, name)
	if err != nil {
		return nil, err
	}
	return &model.ReleaseProfilingResult{
		Status:      result.Status,
		SourceKey:   result.SourceKey,
		ActiveSlots: result.ActiveSlots,
	}, nil
}

// ProfilingSlots is the resolver for the profilingSlots field.
func (r *queryResolver) ProfilingSlots(ctx context.Context) (*model.ProfilingSlots, error) {
	if r.ProfileStore == nil {
		return &model.ProfilingSlots{
			ActiveKeys:          []string{},
			KeysWithData:        []string{},
			TotalBytesUsed:      0,
			SlotMaxBytes:        0,
			MaxSlots:            0,
			MaxTotalBytesBudget: 0,
			SlotTTLSeconds:      0,
		}, nil
	}
	active, withData := r.ProfileStore.DebugSlots()
	stats := r.ProfileStore.MemoryStats()
	return &model.ProfilingSlots{
		ActiveKeys:          active,
		KeysWithData:        withData,
		TotalBytesUsed:      stats.TotalBytes,
		SlotMaxBytes:        stats.SlotMaxBytes,
		MaxSlots:            stats.MaxSlots,
		MaxTotalBytesBudget: stats.MaxTotalBytesBudget,
		SlotTTLSeconds:      stats.SlotTTLSeconds,
	}, nil
}

// SourceProfiling is the resolver for the sourceProfiling field.
func (r *queryResolver) SourceProfiling(ctx context.Context, namespace string, kind string, name string) (*model.SourceProfilingResult, error) {
	if r.ProfileStore == nil {
		return nil, fmt.Errorf("profiling is not available")
	}
	profileOut, err := profiles.GetProfilingForSource(r.ProfileStore, namespace, kind, name)
	if err != nil {
		return nil, err
	}
	profileBytes, err := json.Marshal(profileOut.Profile)
	if err != nil {
		return nil, err
	}
	return &model.SourceProfilingResult{
		ProfileJSON: string(profileBytes),
	}, nil
}
