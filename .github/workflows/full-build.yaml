name: full odigos build

on:
  pull_request:
    branches:
      - 'releases/**'
    types:
      - opened
      - synchronize
      - reopened
  schedule:
    - cron: '0 0 * * *' # Nightly run at midnight

jobs:
  build-all-services:
    name: build-all-services
    strategy:
      matrix:
        service: [autoscaler, scheduler, instrumentor, odigos-collector, odiglet, agents, frontend, operator]
        include:
          - service: autoscaler
            dockerfile_path: .
          - service: scheduler
            dockerfile_path: .
          - service: instrumentor
            dockerfile_path: .
          - service: odigos-collector
            dockerfile_path: collector
          - service: odiglet
            dockerfile_path: odiglet
          - service: agents
            dockerfile_path: odiglet
          - service: frontend
            dockerfile_path: frontend
          - service: operator
            dockerfile_path: operator
    runs-on: depot-ubuntu-24.04-8
    steps:
      - uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      - name: Build ${{ matrix.service }} Image
        uses: docker/build-push-action@v7
        with:
          platforms: linux/amd64,linux/arm64
          push: false
          tags: ${{ matrix.service }}:pr-${{ github.event.number }}
          provenance: false
          file: ${{ matrix.dockerfile_path }}/Dockerfile
          context: .
          build-args: |
            SERVICE_NAME=${{ matrix.service }}
            ODIGOS_VERSION=v0.0.0-pr${{ github.event.number }}
            AGENTS_REGISTRY=p0xd21zf5r.registry.depot.dev
          target: ${{ matrix.service == 'agents' && 'agents' || '' }}

  build-cli:
    runs-on: depot-ubuntu-24.04-8
    steps:
      - uses: actions/checkout@v5
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.2"
      - name: Set up Goreleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          version: latest
          args: build --snapshot --clean
