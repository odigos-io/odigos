issuer: https://token.actions.githubusercontent.com
subject: repo:odigos-io/odigos:ref:refs/heads/main
claim_pattern:
  event_name: workflow_dispatch

permissions:
  contents: write
  pull_requests: write
