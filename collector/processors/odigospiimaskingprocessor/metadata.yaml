type: odigospiimasking
status:
  class: processor
  stability:
    alpha: [traces]
  distributions: [odigos]
  codeowners:
    active: [blumamir]

tests:
  # Processor.Start requires odigos_config_extension on the host, which mdatagen's NopHost cannot provide.
  skip_lifecycle: true
