type: odigosrouterconnector
status:
  class: connector
  stability:
    alpha: [traces_to_traces, metrics_to_metrics, logs_to_logs]
  codeowners:
    active: [tamirdavid]
    seeking_new: false

tests:
  config:
    odigos_k8s_resources_extension_id: odigos_k8s_resources
