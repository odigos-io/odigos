apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-fixed-span-generator
  namespace: load-test
  labels:
    app: go-fixed-span-generator
    odigos-target: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-fixed-span-generator
      odigos-target: "true"
  template:
    metadata:
      labels:
        app: go-fixed-span-generator
        odigos-target: "true"
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: go-fixed-span-generator
          image: public.ecr.aws/odigos/go-fixed-span-gen:v1.0.0
          imagePullPolicy: Always
          env:
            - name: TOTAL_SPANS
              value: "10000"
            - name: SPAN_BYTES
              value: "2000"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            exec:
              command:
                - pgrep
                - main
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pgrep
                - main
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: go-fixed-span-generator-service
  namespace: load-test
  labels:
    app: go-fixed-span-generator
spec:
  selector:
    app: go-fixed-span-generator
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
