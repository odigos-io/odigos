apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-fixed-span-generator
  namespace: load-test
  labels:
    app: java-fixed-span-generator
    odigos-target: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-fixed-span-generator
      odigos-target: "true"
  template:
    metadata:
      labels:
        app: java-fixed-span-generator
        odigos-target: "true"
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: java-fixed-span-generator
          image: public.ecr.aws/odigos/java-fixed-span-gen:v1.0.0
          imagePullPolicy: Always
          env:
            - name: TOTAL_SPANS
              value: "10000"
            - name: SPAN_BYTES
              value: "2000"
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            exec:
              command:
                - pgrep
                - java
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pgrep
                - java
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: java-fixed-span-generator-service
  namespace: load-test
  labels:
    app: java-fixed-span-generator
spec:
  selector:
    app: java-fixed-span-generator
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
