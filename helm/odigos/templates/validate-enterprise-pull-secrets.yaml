{{ include "odigos.validateEnterpriseRegistryPullSecrets" . }}
{{- if and .Values.marketplace.enabled (or .Values.onPremToken .Values.externalOnpremTokenSecret .Values.externalOnpremPullSecret) -}}
{{- fail "Marketplace activation cannot be combined with Odigos token or registry-secret activation" -}}
{{- end -}}
{{- if and .Values.marketplace.enabled .Values.insights.enabled -}}
{{- fail "Marketplace Insights delivery is not available in this release" -}}
{{- end -}}
