{
  "$schema": "https://json-schema.org/draft-07/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "clusterName": {
      "type": "string",
      "description": "Cluster name attached to telemetry."
    },
    "logLevel": {
      "type": "string",
      "enum": ["debug", "info", "warn", "error"],
      "description": "Default log level for Odigos components."
    },
    "marketplace": {
      "type": "object",
      "additionalProperties": false,
      "description": "AWS contract activation settings. Configure IAM roles separately; do not provide credentials.",
      "properties": {
        "licenseManagerRegion": {
          "type": "string",
          "pattern": "^[a-z]{2}-[a-z]+-[0-9]+$",
          "description": "AWS region containing the buyer's License Manager entitlement."
        }
      }
    }
  }
}
