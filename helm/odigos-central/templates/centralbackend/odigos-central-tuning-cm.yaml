{{- if (include "odigos.secretExists" .) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: odigos-central-tuning
  namespace: {{ .Release.Namespace }}
  labels:
    odigos.io/system-object: "true"
data:
  wsTargetPerPod: "{{ .Values.centralBackend.autoscaling.wsTargetPerPod | default 200 }}"
{{- end }}


