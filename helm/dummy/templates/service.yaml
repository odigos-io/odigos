apiVersion: v1
kind: Service
metadata:
  name: {{ include "odigos.fullname" . }}
  labels:
    {{- include "odigos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odigos.selectorLabels" . | nindent 4 }}
