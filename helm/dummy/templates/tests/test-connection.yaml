apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odigos.fullname" . }}-test-connection"
  labels:
    {{- include "odigos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odigos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
