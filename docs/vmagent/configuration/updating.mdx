---
title: "Updating Agent Configuration"
sidebarTitle: "Reloading"
---

To reload the agent configuration after changing the configuration file, run:

```bash
systemctl reload odigos-vmagent
```

To verify the agent after updating the configuration:

```bash
systemctl status odigos-vmagent
```

And check that the agent "Active" state is `active (running)`.
