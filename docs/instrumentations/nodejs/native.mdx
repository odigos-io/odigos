---
title: 'Automatic Instrumentation'
sidebarTitle: 'Native'
icon: 'node-js'
---

<Info>
  Native Instrumentation is currently the default setting of Odigos **Open-Source** version.

  For eBPF-Based Instrumentation [click here](/instrumentations/nodejs/ebpf).
</Info>

## Supported Versions

Odigos uses the official OpenTelemetry instrumentation libraries and SDK components, thus it supports the same Node.js versions as the OpenTelemetry Node.js.

- Node.js runtime versions **14** and above are supported.

## Traces

Odigos will automatically instrument your Node.js sources to record and collect spans for distributed tracing, by utilizing the OpenTelemetry Node.js Official Instrumentation Libraries.

If you use one of the supported logger libraries, the trace id and span id will be auto-injected into your log records allowing signals correlations between logs and traces.

## Instrumentation Libraries

The following npm packages will be auto instrumented by Odigos:

### Node.js Core Modules:

 
 
 
 
 


### HTTP Frameworks:

- [`connect`](https://www.npmjs.com/package/connect) versions `>=0.38.0` 
- [`express`](https://www.npmjs.com/package/express) versions `>=0.41.1` 
- [`fastify`](https://www.npmjs.com/package/fastify) versions `>=0.38.0` 
- [`graphql`](https://www.npmjs.com/package/graphql) versions `>=0.42.0` 
 
- [`koa`](https://www.npmjs.com/package/koa) versions `>=0.42.0` 
 
 
 
 
- [`restify`](https://www.npmjs.com/package/restify) versions `>=0.40.0` 
- [`router`](https://www.npmjs.com/package/router) versions `>=0.39.0` 


### HTTP Clients:

 
 
- [`undici`](https://www.npmjs.com/package/undici) versions `>=0.4.0` 


### RPC (Remote Procedure Call):

- [`aws-sdk`](https://www.npmjs.com/package/aws-sdk) versions `>=0.43.1` 
 
 


### Messaging Systems Clients:

- [`amqplib`](https://www.npmjs.com/package/amqplib) versions `>=0.40.0` 
- [`kafkajs`](https://www.npmjs.com/package/kafkajs) versions `>=0.0.6` 
 
 
- [`socket.io`](https://www.npmjs.com/package/socket.io) versions `>=0.41.0` 


### Database Clients, ORMs, and data access libraries:

- [`aws-sdk`](https://www.npmjs.com/package/aws-sdk) versions `>=0.43.1` 
 
- [`cassandra-driver`](https://www.npmjs.com/package/cassandra-driver) versions `>=0.40.0` 
- [`dataloader`](https://www.npmjs.com/package/dataloader) versions `>=0.11.0` 
- [`generic-pool`](https://www.npmjs.com/package/generic-pool) versions `>=0.38.0` 
- [`ioredis`](https://www.npmjs.com/package/ioredis) versions `>=0.42.0` 
- [`knex`](https://www.npmjs.com/package/knex) versions `>=0.39.0` 
- [`lru-memoizer`](https://www.npmjs.com/package/lru-memoizer) versions `>=0.39.0` 
- [`memcached`](https://www.npmjs.com/package/memcached) versions `>=0.38.0` 
- [`mongodb`](https://www.npmjs.com/package/mongodb) versions `>=0.46.0` 
- [`mongoose`](https://www.npmjs.com/package/mongoose) versions `>=0.40.0` 
- [`mysql`](https://www.npmjs.com/package/mysql) versions `>=0.40.0` 
- [`mysql2`](https://www.npmjs.com/package/mysql2) versions `>=0.40.0` 
 
- [`pg`](https://www.npmjs.com/package/pg) versions `>=0.43.0` 
- [`redis`](https://www.npmjs.com/package/redis) versions `>=0.41.0` 
- [`tedious`](https://www.npmjs.com/package/tedious) versions `>=0.12.0` 


### Loggers:

Automatic injection of trace context (trace id and span id) into log records for the following loggers:

- [`bunyan`](https://www.npmjs.com/package/bunyan) versions `>=0.40.0` 
- [`pino`](https://www.npmjs.com/package/pino) versions `>=0.41.0` 
- [`winston`](https://www.npmjs.com/package/winston) versions `>=0.39.0` 


### Other:

- [`dns`](https://www.npmjs.com/package/dns) versions `>=0.38.0` 
- [`fs`](https://www.npmjs.com/package/fs) versions `>=0.14.0` 
- [`grpc`](https://www.npmjs.com/package/grpc) versions `>=0.52.1` 
- [`hapi`](https://www.npmjs.com/package/hapi) versions `>=0.40.0` 
- [`http`](https://www.npmjs.com/package/http) versions `>=0.52.1` 
- [`nestjs-core`](https://www.npmjs.com/package/nestjs-core) versions `>=0.39.0` 
- [`net`](https://www.npmjs.com/package/net) versions `>=0.38.0` 
- [`redis-4`](https://www.npmjs.com/package/redis-4) versions `>=0.41.0` 

{/* END OF FILE */}