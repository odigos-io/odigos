---
title: 'Pyroscope'
description: 'Configuring the Pyroscope backend (Self-Hosted)'
sidebarTitle: 'Pyroscope'
icon: 'signal-stream'
---

### Getting Started

<img src="https://d15jtxgb40qetw.cloudfront.net/pyroscope.svg" alt="pyroscope" className="not-prose h-20" />

{/*
    Add custom content here (under this comment)...

    e.g.

    **Creating Account**<br />
    Go to the **[🔗 website](https://odigos.io) > Account** and click **Sign Up**

    **Obtaining Access Token**<br />
    Go to **⚙️ > Access Tokens** and click **Create New**

    !! Do not remove this comment, this acts as a key indicator in `docs/sync-dest-doc.py` !!
    !! START CUSTOM EDIT !!
*/}

[Grafana Pyroscope](https://grafana.com/oss/pyroscope/) is an open-source continuous profiling backend. Odigos exports profile data over OTLP to Pyroscope so the same eBPF profiler that powers in-product views can also feed long-term, queryable storage.

<Note>
  This destination only emits the **Profiles** signal. The destination must include `PROFILES`, and VM profiling must be enabled — see [Continuous profiling overview](/vmagent/instrumentations/profiling/overview) and [getting started](/vmagent/instrumentations/profiling/getting-started).
</Note>

**Pyroscope version**<br />
Use a Pyroscope build that accepts OTLP profiles on the endpoint you configure. If you are unsure, upgrade to a recent release before configuring Odigos.

**Quickstart: self-hosted Pyroscope**<br />
If you don't already run Pyroscope, the upstream Helm chart gets you a working instance in a couple of commands:

```bash
helm repo add grafana https://grafana.github.io/helm-charts
helm repo update
helm install pyroscope grafana/pyroscope \
  --namespace pyroscope --create-namespace
```

The distributor will be reachable in-cluster at `pyroscope.pyroscope:4040` — use that as the `PYROSCOPE_ENDPOINT` value below.

<Warning>
  Provide the distributor's `host:port` only. Do **not** add `/otlp` or other path suffixes unless your Pyroscope deployment requires them.
</Warning>

**TLS**<br />
Toggle `PYROSCOPE_TLS_ENABLED` when Pyroscope is fronted by TLS. For plain in-cluster traffic, leave it off.

**Verifying the export**<br />
After applying the destination and sending traffic to a profiled source, open the Pyroscope UI (`kubectl -n pyroscope port-forward svc/pyroscope 4040`) and confirm that profiles appear under your service.

{/*
    !! Do not remove this comment, this acts as a key indicator in `docs/sync-dest-doc.py` !!
    !! END CUSTOM EDIT !!
*/}

### Configuring Destination Fields

<Accordion title="Supported Signals:">
  ❌ Traces
  ❌ Metrics
  ❌ Logs
  ✅ Profiles
</Accordion>

- **PYROSCOPE_ENDPOINT** `string` : Endpoint. Pyroscope OTLP gRPC endpoint. Format: host:port (default port 4040).
  - This field is required
  - Example: `host:4040`
- **PYROSCOPE_TLS_ENABLED** `boolean` : Enable TLS. Enable TLS for the gRPC connection to Pyroscope.
  - This field is optional and defaults to `False`
- **PYROSCOPE_CA_PEM** `string` : Certificate Authority. CA certificate in PEM format to verify the Pyroscope server. Leave empty to use system root CA.
  - This field is optional
  - Example: `-----BEGIN CERTIFICATE-----`
- **PYROSCOPE_INSECURE_SKIP_VERIFY** `boolean` : Insecure Skip Verify. Skip TLS certificate verification.
  - This field is optional and defaults to `False`

### Adding Destination to Odigos

There are two primary methods for configuring destinations in Odigos:

##### **Using the UI**

<Steps>
  <Step>
    Use the [Odigos CLI](https://docs.odigos.io/cli/odigos_ui) to access the UI
    ```bash
    odigos ui
    ```
  </Step>
  <Step>
    Click on `Add Destination`, select `Pyroscope` and follow the on-screen instructions
  </Step>
</Steps>

##### **Using Kubernetes manifests**

<Steps>
  <Step>
    Save the YAML below to a file (e.g. `pyroscope.yaml`)
    ```yaml
    apiVersion: odigos.io/v1alpha1
    kind: Destination
    metadata:
      name: pyroscope-example
      namespace: odigos-system
    spec:
      data:
        PYROSCOPE_ENDPOINT: <Endpoint>
        # Note: The commented fields below are optional.
        # PYROSCOPE_TLS_ENABLED: <Enable TLS>
        # PYROSCOPE_CA_PEM: <Certificate Authority>
        # PYROSCOPE_INSECURE_SKIP_VERIFY: <Insecure Skip Verify>
      destinationName: pyroscope
      signals:
      - PROFILES
      type: pyroscope
    ```
  </Step>
  <Step>
    Apply the YAML using `kubectl`
    ```bash
    kubectl apply -f pyroscope.yaml
    ```
  </Step>
</Steps>